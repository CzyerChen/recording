package learningpattern.springdesignpattern;

/**
 * 对象池模式：通过创建一个巨型对象，每次使用对象的时候，不是新建对象，而是通过利用现有已经创建的对象
 * Spring一个很好的实践是包括数据库连接池，就是利用了对象池的思想
 * 数据库连接池不是Spring实现的，是druid c3p0 dbcp实现的
 */
public class ObjectPoolModel {
}
